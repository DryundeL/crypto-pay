package main

import (
	"log/slog"
	"os"
)

// Scanner process entrypoint (Blockchain context).
// Will observe EVM + Bitcoin networks and emit TransactionObserved events.
func main() {
	slog.Info("scanner stub — not implemented yet")
	os.Exit(0)
}
